<%= form_for @post, html: { multipart: true }  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title, placeholder: 'Hello World!' %>

  <br/>

  <%= f.label :body %>
  <%= f.text_area :body %>

  <%= f.file_field :image %>

  <%= f.submit 'Submit' %>
<% end %>
